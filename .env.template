# Path to Stable Diffusion extensions directory. If set, `make build` and `make watch` commands write the
# build results there, under a directory named after the extension.
SD_WEB_UI_EXTENSIONS_PREFIX=""
